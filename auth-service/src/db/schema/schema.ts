import { jwtKeysTable } from "./jwtKeys";
import { refreshTokenTable } from "./refreshToken";
import { userTable } from "./user";

export const schema = {
    userTable,
    refreshTokenTable,
    jwtKeysTable
}